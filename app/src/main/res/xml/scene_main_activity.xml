<?xml version="1.0" encoding="utf-8"?>
<MotionScene
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <Transition
        app:constraintSetEnd="@id/expanded"
        app:constraintSetStart="@id/collapsed"
        app:duration="200">

        <!--TODO OnSwipe seems to apply to the entire layout, which we don't want.
        Work around by setting a touch listener on the view-->
    </Transition>

    <ConstraintSet android:id="@+id/collapsed">
        <!--<Constraint-->
            <!--android:id="@id/nowPlayingView"-->
            <!--android:layout_width="match_parent"-->
            <!--android:layout_height="wrap_content"-->
            <!--app:layout_constraintTop_toTopOf="@id/preview_space" />-->
    </ConstraintSet>

    <ConstraintSet android:id="@+id/expanded">
        <!--<Constraint-->
            <!--android:id="@id/nowPlayingView"-->
            <!--android:layout_width="match_parent"-->
            <!--android:layout_height="wrap_content"-->
            <!--app:layout_constraintBottom_toBottomOf="@id/preview_space" />-->
    </ConstraintSet>
</MotionScene>